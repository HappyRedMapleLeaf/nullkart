overall:
clean up ble stuff
add init functions
fix naming
comment

make math utils less repetitive and use "templates" for type?? (vec2, vec3, vec4)

localization telemetry

host:

mcu:

general code:

mechanical:
encoder connectors to motors keep getting loose. ask jeff to crimp another one